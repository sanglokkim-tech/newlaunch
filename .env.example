# Server-side only — never prefix with NEXT_PUBLIC_
# Rotate these IDs if they were ever committed to git history.

# Formspree form ID for waitlist submissions
FORMSPREE_WAITLIST=your_waitlist_form_id

# Formspree form ID for feedback submissions
FORMSPREE_FEEDBACK=your_feedback_form_id
